# Pinged – React Native (Expo) Setup

## Requirements
- Node.js 18+
- Expo CLI
- Android Studio or Xcode (for emulators)

## Setup Instructions
1. Clone the repo:
   ```bash
   git clone <repo-url>
   cd pinged
